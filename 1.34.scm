#lang racket
; ///////////////////////////////////1.34///////////////////////////////
; 如果我们定义了一个过程, (define (f g) (g 2))
; 然后,调用(f square)=(square 2)=4
; 也可以定义，(f (lambda (z) (* z (+ z 1))))=6
; 那么(f f)的结果是怎么样的呢？

(define (f g) (g 2))
(f f)
; 答案是：抛出异常“2不是一个过程”
; (f f)=>(f 2)=>(2 2)